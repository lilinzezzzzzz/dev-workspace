interface:
  display_name: "Git Checkout Branch"
  short_description: "先确认基础分支并按远程跟踪分支解析，再基于当前 diff 推断分支名"
  default_prompt: "Use $git-checkout-branch to first ask me which base branch to use, follow the shared remote-base rule, inspect the current branch and diff, recommend one branch name from the dominant change, and switch only after I confirm."
